export const metadata = {
  title: 'Sanity Studio | WellnessReal',
  description: 'Panel de administración del blog',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
